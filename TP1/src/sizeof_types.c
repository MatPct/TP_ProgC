#include <stdio.h>
#include <string.h>

int main() {
  printf("Taille des types de base en C (en octets) :");
  
  printf("char : %s octet(s)\n", sizeof(char));
  printf("signed char : %s octet(s)\n", sizeof(signed char));
  printf("unsigned char : %s octet(s)\n", sizeof(unsigned char));
}








